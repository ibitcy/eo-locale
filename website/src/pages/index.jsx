import React from 'react';
import classnames from 'classnames';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: <>Friendly API</>,
    imageUrl: 'img/icons8-laurel-wreath-96.png',
    description: (
      <>
        eo-locale inspired by react-intl. Most popular internationalization library in React community
      </>
    ),
  },
  {
    title: <>Tiny size</>,
    imageUrl: 'img/icons8-parachute-96.png',
    description: (
      <>
        1.9 kB gzipped size. It is very important to provide small client javascript bundles for a best user experience.
      </>
    ),
  },
  {
    title: <>Strong abilities</>,
    imageUrl: 'img/icons8-trophy-96.png',
    description: (
      <>
        Within eo-locale you can format dates, numeric values and plural values.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={classnames('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3 className="text--center">{title}</h3>
      <p>{description}</p>
    </div>
  );
}

function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title="Internationalization of React applications"
      description="Internationalization of React applications. Elegant lightweight library based on Internationalization API">
      <header className={classnames('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={classnames(
                'button button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/react/intro')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map((props, idx) => (
                  <Feature key={idx} {...props} />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}

export default Home;
